// TODO: Exercice Module 04 — ecrire un prompt pour que Claude implemente ce composant
//
// Ce composant doit afficher un dropdown avec les 3 franchises
// (Nantes Centre, Rennes Nord, Brest Ocean) + une option "Toutes".
// Quand l'utilisateur selectionne une franchise, appeler onFilter(franchise).
// Quand il selectionne "Toutes", appeler onFilter(null).

export default function FranchiseFilter({ onFilter }) {
  return null
}
